<div>
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                {{#link-to "index" classNames="navbar-brand"}}Sistearth{{/link-to}}
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li>{{#link-to "posts" }}Actualités{{/link-to}}</li>
                </ul>

                <div class="navbar-form navbar-right">
                    {{#if session.isAuthenticated}}
                        <a {{action 'invalidateSession'}} class="btn btn-primary">Déconnexion</a>
                    {{else}}
                        {{#link-to 'login' classNames="btn btn-primary"}}Connexion{{/link-to}}
                    {{/if}}
                </div>
            </div>
            <!--/.nav-collapse -->
        </div>
        <!--/.navbar -->
    </div>

    <div class="container" id="main">
        <div class="row">
            <div class="col-md-12">
                {{outlet}}
            </div>
        </div>
    </div>
</div>

