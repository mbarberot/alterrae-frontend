<div class="row">
  <h1 class="page-header">Inscription</h1>

  <form role="form" class="form-horizontal col-md-12" {{action 'register' on='submit' }}>
    {{#if errorMessage}}
      <div class="alert alert-danger">
        <p>
          {{errorMessage}}
        </p>
      </div>
    {{/if}}

    {{#auto-validated-input label="Identifiant" errors=errors.username}}
      {{input type='text' classNames='form-control' id='username' placeholder='Identifiant' value=username}}
    {{/auto-validated-input}}

    <hr/>

    {{#auto-validated-input label="Mot de passe" errors=errors.password}}
      {{input type='password' classNames='form-control' id='password' placeholder='Mot de passe' value=password}}
    {{/auto-validated-input}}

    {{#auto-validated-input label="Confirmation" errors=errors.passwordConfirmation}}
      {{input type='password' classNames='form-control' id='password-confirmation' placeholder='Confirmation du mot de passe' value=passwordConfirmation}}
    {{/auto-validated-input}}

    <hr/>

    {{#auto-validated-input label="E-mail" errors=errors.email}}
      {{input type='email' classNames='form-control' id='email' placeholder='email@example.com' value=email}}
    {{/auto-validated-input}}

    {{#auto-validated-input label="Confirmation" errors=errors.emailConfirmation}}
      {{input type='email' classNames='form-control' id='email-confirmation' placeholder='email@example.com' value=emailConfirmation}}
    {{/auto-validated-input}}

    <hr/>
    <div class="form-group">
      <div class="col-sm-8 col-sm-offset-4">
        <button id="submit" type="submit" class="btn btn-primary col-sm-4" disabled={{isInvalid}}>Valider
        </button>
      </div>
    </div>
  </form>
</div>
<div class="modal fade" id="successModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">
          <span aria-hidden="true">&times;</span>
          <span class="sr-only">Close</span>
        </button>
        <h4 class="modal-title" id="myModalLabel">Inscription réussie !</h4>
      </div>
      <div class="modal-body">
        {{#link-to 'login' class="btn btn-default" data-dismiss="modal" }}Fermer{{/link-to}}
      </div>
    </div>
  </div>
</div>
