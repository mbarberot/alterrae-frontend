<div class="row">
  <h1 class="page-header">
    Changer le mot de passe
    <div class="pull-right">
      {{#link-to 'account.index' class="btn btn-default" }}
        <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>
        Retour
      {{/link-to}}
    </div>
  </h1>

  <form class="form-horizontal" {{action 'update' on='submit'}}>
    {{#if controller.errorMessage}}
      <div class="alert alert-danger">
        <p>
          {{controller.errorMessage}}
        </p>
      </div>
    {{/if}}
    {{#auto-validated-input label="Nouveau mot de passe" errors=controller.errors.password}}
      {{input type='password' classNames='form-control' id='new-password' placeholder='nouveau mot de passe' value=controller.password}}
    {{/auto-validated-input}}
    {{#auto-validated-input label="Confirmation" errors=controller.errors.passwordConfirmation}}
      {{input type='password' classNames='form-control' id='new-password-confirmation' placeholder='nouveau mot de passe' value=controller.passwordConfirmation}}
    {{/auto-validated-input}}
    {{#auto-validated-input label="Mot de passe actuel" errors=controller.errors.actualPassword}}
      {{input type='password' classNames='form-control' id='actual-password' placeholder='mot de passe actuel' value=controller.actualPassword}}
    {{/auto-validated-input}}

    <div class="form-group">
      <div class="col-sm-5 col-sm-offset-3">
        <div class="btn-group col-sm-6 col-sm-offset-3">
          <button id="submit" type="submit" class="btn btn-warning" {{bind-attr disabled="controller.isInvalid"}}>Valider</button>
          {{#link-to 'account' class="btn btn-info" }}Annuler{{/link-to}}
        </div>
      </div>
    </div>
  </form>
</div>
